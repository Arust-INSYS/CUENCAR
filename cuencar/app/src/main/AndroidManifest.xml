<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rentcar.cuencar" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cuencar"
        tools:targetApi="31" >
        <activity
            android:name=".alquiler"
            android:exported="false" />
        <activity
            android:name=".editar_perfil"
            android:exported="false" />
        <activity
            android:name=".Perfil"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            />
        <activity
            android:name=".MenuPrincipal"
            android:exported="false" />
        <activity
            android:name=".reservas_pendientes"
            android:exported="false" />
        <activity
            android:name=".mis__reservas"
            android:exported="false" />
        <activity
            android:name=".AceptarTerminos"
            android:exported="false" />
        <activity
            android:name=".ConectarGmail"
            android:exported="false" />
        <activity
            android:name=".Registro"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CODIGO DAVID -->
        <activity
            android:name=".Alquiler_Vehiculo"
            android:exported="false" />
        <activity
            android:name=".Informacion"
            android:exported="false" />

        <!-- CODIGO ARIEL -->
        <activity
            android:name=".catalogo"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Informacion_auto"
            android:exported="false" />
        <activity
            android:name=".confirmacion_datos"
            android:exported="false" />
        <activity
            android:name=".transaccion"
            android:exported="false" />

    </application>

</manifest>